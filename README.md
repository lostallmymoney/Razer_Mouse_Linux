# Razer Mouse Linux – Log Events Branch

Install this branch with:

```
sudo apt install unzip wget -y && cd ~ && rm -rf ~/Razer_Mouse_Linux.zip && wget https://codeload.github.com/lostallmymoney/Razer_Mouse_Linux/zip/refs/heads/log-events -O ~/Razer_Mouse_Linux.zip && unzip -o ~/Razer_Mouse_Linux.zip -d ~ && rm -rf ~/Razer_Mouse_Linux.zip && cd ~/Razer_Mouse_Linux-log-events && sh install.sh
```

After installing, run:

```
naga debug
```
